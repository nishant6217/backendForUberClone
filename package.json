{
  "name": "assignmentsmarth",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "seed": "npx sequelize-cli db:seed:all",
    "seed-data": "npx sequelize-cli seed:generate --name booking",
    "migrate": "npx sequelize-cli db:migrate",
    "down": "npx sequelize-cli db:migrate:undo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "nodemon": "^2.0.20",
    "path": "^0.12.7",
    "pg": "^8.8.0",
    "sequelize": "^6.25.3",
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "sequelize-cli": "^6.5.2"
  }
}
